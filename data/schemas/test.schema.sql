CREATE TABLE a (
    i INT,
    j INT,
    k INT
);

CREATE TABLE b (
x INT,
y INT,
z INT
);

CREATE TABLE c (
u INT,
v CHAR(10),
w DECIMAL(1, 10)
);

CREATE TABLE d (
p INT,
q CHAR(10),
r DECIMAL(1, 10)
);
